spring:
  datasource:
      driver-class-name : org:postgresql:Driver
      url: jdbc:postgresql://localhost:5432/tasks
      username: postgres
      password: postgres
      hikari:
        schema: tasks-schema
